# Backend REST API base URL (must include /api)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Stripe publishable key — leave empty to use the backend's mock payment fallback
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
